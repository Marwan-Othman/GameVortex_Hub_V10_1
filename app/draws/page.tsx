import {db} from '../../lib/prisma';export const dynamic='force-dynamic';export default async function Draws(){const draws=await db.raffle.findMany({where:{status:{in:['OPEN','DRAWN']}},orderBy:{drawAt:'asc'},take:20});return <main className="wrap"><section className="hero"><div className="eyebrow">VORTEX DRAWS</div><h1>السحوبات والجوائز.</h1><p>سحوبات مستقلة يديرها GameVortex. التذاكر والحالة والفائز تحفظ في قاعدة بيانات المشروع.</p></section><div className="grid" style={{marginTop:20}}>{draws.map(d=><article className="card" key={d.id}><span className="badge">{d.status}</span><h2>{d.title}</h2><p className="muted">{d.description}</p><p><strong>الجائزة:</strong> {d.prize}</p><p className="muted">تكلفة التذكرة: {d.ticketCost} نقطة</p></article>)}{!draws.length&&<div className="card"><h2>لا توجد سحوبات مفتوحة حاليًا</h2><p className="muted">يمكن للمالك إنشاء السحوبات من لوحة الإدارة بعد تجهيز قواعد الجوائز.</p></div>}</div></main>}
